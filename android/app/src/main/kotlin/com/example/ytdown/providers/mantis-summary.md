# Security Summary: android/app/src/main/kotlin/com/example/ytdown/providers

## Directory: android/app/src/main/kotlin/com/example/ytdown/providers


### Local Source Files


#### BrowserProvider.kt

Declarations:

- `data class BrowserState(`

- `fun copyWith(`

- `class BrowserProvider {`

- `fun setUrl(url: String) {`

- `fun setLoading(isLoading: Boolean) {`

- `fun setProgress(progress: Double) {`

- `fun setInitialLoad(isInitialLoad: Boolean) {`


#### DownloadDiagnosticsProvider.kt

Declarations:

- `class DownloadDiagnosticsProvider @Inject constructor(`

- `fun retryDownload(item: DownloadItemEntity) {`


#### DownloadProvider.kt

Declarations:

- `class DownloadProvider {`

- `fun update(items: List<DownloadItemEntity>) {`

- `fun addOrUpdate(item: DownloadItemEntity) {`

- `fun remove(id: String) {`

- `fun clear() {`


#### HomeProvider.kt

Declarations:

- `data class HomeUiState(`

- `class HomeProvider : ViewModel() {`

- `fun setLoading(isLoading: Boolean) {`

- `fun setProcessing(isProcessing: Boolean) {`

- `fun setError(error: String?) {`

- `fun setUrl(url: String) {`

- `fun clearError() {`


#### LibraryPlaylistSelectionProvider.kt

Declarations:

- `data class LibraryPlaylistSelectionState(`

- `class LibraryPlaylistSelectionProvider : ViewModel() {`

- `fun selectOnly(trackId: String) {`

- `fun toggleSelection(trackId: String) {`

- `fun clearSelection() {`


#### LibraryPlaylistsProvider.kt

Declarations:

- `class LibraryPlaylistsProvider @Inject constructor(`

- `fun refresh() {`

- `fun createPlaylist(name: String, description: String? = null) {`

- `fun addTracksToPlaylist(playlistId: String, trackIds: List<String>) {`


#### LibraryProvider.kt

Declarations:

- `class LibraryProvider @Inject constructor(`

- `fun searchLibrary(query: String): Flow<List<DownloadItemEntity>> {`

- `fun getPlaylistTracks(playlistId: String) = libraryRepository.getPlaylistTracks(playlistId)`

- `fun addTrackToPlaylist(playlistId: String, trackId: String) {`


#### PlayerProvider.kt

Declarations:

- `class PlayerProvider @Inject constructor(`

- `fun playTrack(item: DownloadItemEntity) = manager.playTrack(item)`

- `fun playPlaylist(items: List<DownloadItemEntity>, startIndex: Int = 0) = manager.playPlaylist(items, startIndex)`

- `fun pause() = manager.pause()`

- `fun resume() = manager.resume()`

- `fun next() = manager.next()`

- `fun previous() = manager.previous()`

- `fun seekTo(position: Long) = manager.seekTo(position)`

- `fun toggleRepeatMode() = manager.toggleRepeatMode()`

- `fun toggleShuffle() = manager.toggleShuffle()`


#### PlaylistDetailProvider.kt

Declarations:

- `class PlaylistDetailProvider @Inject constructor(`

- `fun setPlaylistItems(items: List<VideoPreviewItem>) {`

- `fun onSearchQueryChanged(query: String) {`

- `fun toggleSelection(item: VideoPreviewItem) {`

- `fun selectAll(select: Boolean) {`


#### SharingProvider.kt

Declarations:

- `class SharingProvider @Inject constructor(`

- `fun extractSharedUrl(intent: Intent): String? {`

- `fun cleanUrl(url: String): String {`


---
*Generated by mantis-summarize*